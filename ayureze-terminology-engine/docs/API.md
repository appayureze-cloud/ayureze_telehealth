# API

OpenAPI docs are auto-generated by FastAPI at `/docs` (Swagger UI) and
`/openapi.json` once the service is running — this document gives real,
captured example responses instead of duplicating the schema.

## `GET /health`

```json
{"status": "ok", "database": "ok"}
```

## `GET /v1/terminology/search?q=Guduchi` — real captured response

```json
{
  "query": "Guduchi",
  "matches": [
    {"concept_id": "AYU-HERB-000004", "canonical_name": "Giloy", "category": "HERB", "domain": "AYURVEDA", "match_type": "exact_synonym", "confidence": 0.9, "matched_name": "Guduchi"},
    {"concept_id": "AYU-HERB-000078", "canonical_name": "Amrita", "category": "HERB", "domain": "AYURVEDA", "match_type": "exact_synonym", "confidence": 0.9, "matched_name": "Guduchi"},
    {"concept_id": "AYU-FORMULATION-000153", "canonical_name": "Kamadugha Rasa (with Mukta)", "category": "FORMULATION", "domain": "AYURVEDA", "match_type": "prefix", "confidence": 0.6, "matched_name": "Guduchi Satva"},
    {"concept_id": "AYU-FORMULATION-000091", "canonical_name": "Amritottaram Kashayam", "category": "FORMULATION", "domain": "AYURVEDA", "match_type": "fuzzy", "confidence": 0.53333336, "matched_name": "Amrita (Guduchi)"},
    {"concept_id": "AYU-MEDICINE-001352", "canonical_name": "Brihat Guduchi taila", "category": "MEDICINE_TERM", "domain": "AYURVEDA", "match_type": "fuzzy", "confidence": 0.3809524, "matched_name": "Brihat Guduchi taila"}
  ]
}
```

Note the real, honest ambiguity this surfaces: "Guduchi" resolves to
**two different concept_ids** (Giloy and Amrita), because the source data
itself lists "Guduchi" as a synonym of both — exactly the kind of case
`deduplication_candidates` exists to flag for human review (see
`docs/DATA_QUALITY_REPORT.md`), not something the search API papers over.

## `POST /v1/terminology/resolve` — the spec's own worked example, real response

Request: `{"text": "Guduchi"}`

```json
{
  "query": "Guduchi",
  "matches": [
    {"concept_id": "AYU-HERB-000004", "canonical_name": "Giloy", "category": "HERB", "match_type": "exact_synonym", "confidence": 0.9},
    {"concept_id": "AYU-HERB-000078", "canonical_name": "Amrita", "category": "HERB", "match_type": "exact_synonym", "confidence": 0.9},
    {"concept_id": "AYU-FORMULATION-000153", "canonical_name": "Kamadugha Rasa (with Mukta)", "category": "FORMULATION", "match_type": "prefix", "confidence": 0.6},
    {"concept_id": "AYU-FORMULATION-000091", "canonical_name": "Amritottaram Kashayam", "category": "FORMULATION", "match_type": "fuzzy", "confidence": 0.53333336},
    {"concept_id": "AYU-MEDICINE-001352", "canonical_name": "Brihat Guduchi taila", "category": "MEDICINE_TERM", "match_type": "fuzzy", "confidence": 0.3809524}
  ]
}
```

No LLM is called anywhere in this path — `resolve` is `search` under a
different request/response shape, both backed by the same deterministic
SQL in `terminology/search.py`.

## `GET /v1/terminology/concepts/{concept_id}` — real response for `AYU-HERB-000004`

```json
{"concept_id": "AYU-HERB-000004", "domain": "AYURVEDA", "category": "HERB", "canonical_name": "Giloy", "definition": "Giloy (Tinospora cordifolia) is a powerful immunity booster...", "status": "candidate", "confidence": 1.0}
```

`GET .../names`, `.../sources`, `.../relationships` — each returns real,
traceable data; see `docs/DATA_MODEL.md` for what each field means. A
concept that doesn't exist returns `404` with
`{"detail": "concept 'X' not found"}`, never a 200 with an empty/null body.

## `GET /v1/sources` — all 12 approved sources, real status

Returns every source in `data/manifests/source_manifest.json`, including
the 6 adapter-only biomedical terminologies with `record_count: 0` — see
`docs/INGESTION.md`'s table for the real per-source ingested counts.

## `GET /v1/stats` — real, current numbers

```json
{
  "total_concepts": 5085,
  "concepts_by_domain": {"AYURVEDA": 4858, "BIOMEDICAL": 213, "INTEROP": 14},
  "concepts_by_category": {"AYURVEDIC_CONCEPT": 206, "DISEASE": 213, "FORMULATION": 176, "HERB": 2575, "MEDICINE_TERM": 1536, "NAMASTE_CODE": 14, "PATHOLOGY_TERM": 203, "SIDDHANTA": 162},
  "total_names": 7366,
  "total_relationships": 683,
  "total_source_records": 4872,
  "total_sources": 12,
  "deduplication_candidates_pending": 802,
  "deduplication_candidates_accepted": 0,
  "deduplication_candidates_rejected": 0
}
```

## Security behavior (spec section 23), verified with real requests

- **Query length limit**: `GET /v1/terminology/search?q=<300 chars>` → real
  `422` with `{"detail": "query too long (max 200 characters)"}`.
- **SQL injection**: `q=Tulsi'; DROP TABLE concepts;--` → real `200`,
  treated as a literal (harmless) search string; the `concepts` table is
  untouched (every query in `terminology/search.py` is parameterized —
  grep-verified: zero f-string/`.format()`-built SQL anywhere in that
  module).
- **Rate limiting**: in-memory, per-client-IP sliding window
  (`api/rate_limit.py`), `429` once `TERMINOLOGY_RATE_LIMIT_PER_MINUTE`
  (default 120/min) is exceeded. Known limitation: in-process only, so a
  multi-replica deployment would need a shared store instead — documented,
  not silently pretended away.

## `GET /v1/biomedical/{system}/search?q=` — adapter endpoints (biomedical terminologies)

**Now wired in and exercised for real (2026-09-26)** — `system` is one of
`rxnorm`, `mesh`, `icd11`, `loinc`, `snomed`, `atc`. Live lookup only,
never a bulk local copy (spec section 18) — see `docs/LICENSE_MATRIX.md`
for why each one is in the state it's in.

**`rxnorm` and `mesh` genuinely work today** — real public APIs, no
credentials needed. Real captured responses from this build's own testing:

```
GET /v1/biomedical/rxnorm/search?q=ibuprofen
{"system": "rxnorm", "query": "ibuprofen", "results": [
  {"code": "1100070", "display": "famotidine 26.6 MG / ibuprofen 800 MG Oral Tablet [Duexis]", "source": "RxNorm", "source_url": "https://rxnav.nlm.nih.gov/REST/rxcui/1100070"},
  {"code": "1101919", "display": "ibuprofen 200 MG Oral Tablet [Counteract IB]", "source": "RxNorm", "source_url": "https://rxnav.nlm.nih.gov/REST/rxcui/1101919"},
  ...
]}

GET /v1/biomedical/mesh/search?q=hypertension
{"system": "mesh", "query": "hypertension", "results": [
  {"code": "T034127", "display": "Benign Intracranial Hypertension", "source": "MeSH", "source_url": "http://id.nlm.nih.gov/mesh/T034127"},
  ...
]}
```

**`icd11`, `loinc`, `snomed` correctly return a real `503`** — credentials/
licensing this deployment doesn't have, never a faked result:

```
GET /v1/biomedical/snomed/search?q=diabetes -> 503
{"detail": "SNOMED CT is disabled: no TERMINOLOGY_SNOMED_SERVER_URL configured. ..."}

GET /v1/biomedical/icd11/search?q=diabetes -> 503
{"detail": "ICD-11 adapter requires TERMINOLOGY_ICD11_CLIENT_ID/TERMINOLOGY_ICD11_CLIENT_SECRET ..."}

GET /v1/biomedical/loinc/search?q=diabetes -> 503
{"detail": "LOINC adapter requires TERMINOLOGY_LOINC_USERNAME/TERMINOLOGY_LOINC_PASSWORD ..."}
```

**`atc` always returns `503`, by design** — no automated lookup path
exists at all, per the WHO Collaborating Centre's own published terms:

```
GET /v1/biomedical/atc/search?q=diabetes -> 503
{"detail": "ATC/DDD has no automated lookup path in this codebase, by design: ..."}
```

An unrecognized `system` value returns a real `404` naming the valid
options:

```
GET /v1/biomedical/fakesystem/search?q=test -> 404
{"detail": "unknown biomedical system 'fakesystem'; available: ['atc', 'icd11', 'loinc', 'mesh', 'rxnorm', 'snomed']"}
```

Same query-length limit and rate limiting as `/v1/terminology/search`
(a 300-char query returns `422`, verified). The route only ever
constructs the fixed, hardcoded adapter for a known `system` name — a
caller can never supply an arbitrary URL for this endpoint to fetch
(spec section 23).
