{
  "name": "@ayureze/telehealth-web",
  "version": "0.1.0",
  "description": "Headless Web SDK for AyurEze Telehealth — wraps the Go session API and LiveKit's Web client behind a single interface.",
  "type": "module",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "files": ["dist"],
  "scripts": {
    "build": "tsc -p tsconfig.build.json",
    "typecheck": "tsc --noEmit",
    "test": "vitest run"
  },
  "dependencies": {
    "livekit-client": "^2.7.5"
  },
  "devDependencies": {
    "typescript": "^5.6.2",
    "vitest": "^2.1.8"
  }
}
