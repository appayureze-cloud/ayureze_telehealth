[pytest]
markers =
    integration: requires the live docker-compose stack + a running Go API (see tests/test_agent_integration.py docstring)
    models: loads real ML models (slow, downloads weights on first run) but needs no external services
addopts = -m "not integration and not models"
asyncio_mode = auto
