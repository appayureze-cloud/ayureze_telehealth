# Security Documentation

Threat model, RBAC/tenant-isolation model, and security control inventory
are written up as they are implemented — see `PROGRESS.md` at the repo
root. Populated starting Day 3 (auth/RBAC/tenant isolation) and Day 4
(E2EE, consent, security event logging).
