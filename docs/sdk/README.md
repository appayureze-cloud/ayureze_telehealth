# SDK Usage

Flutter SDK and Web SDK usage docs are written here once implemented on
Day 7. See section 12 of the build spec for the target headless API
surface (`initialize`, `authenticate`, `createSession`, `joinSession`,
`enableAITranslation`, etc.).
